# Normalize all text files to LF in the repo and on checkout, so a
# Windows checkout doesn't fight Prettier (which formats with LF).
* text=auto eol=lf

# Real binary assets — never let git attempt text/EOL conversion.
*.ico binary
